# Python
__pycache__/
*.pyc
*.pyo
venv/

# Data (never commit user data)
data/
!data/.gitkeep
*.jsonl
bench_log.jsonl
comparison/

# Old root-level files (moved to dashboard/)
/server.py
/static/

# Core engine (now in private repo)
/core/

# Local venvs
.venv-mcp/

# Node
node_modules/

# Secrets — never commit
.env
.env.*

# macOS
.DS_Store

# Local project graphs (per-project, stays on machine)
.dual-graph/
.dual-graph-context/
